# 个人日程管理系统 · PRD（第一版）

版本：v2.0
日期：2026-09-20
状态：定稿。**本文档是后续开发的唯一依据。** 旧六模块总纲已存档为 `PRD_总纲_v1.0存档.md`，其中科研/健身/设置与云数据库方案不在第一版范围，留待日后另立版本讨论；需求与交互设计过程稿见 `PRD_第一阶段.md`。

---

## 1. 产品目标和使用场景

**目标**：一名大学生管理自己的课程与日程：打开网页即可看今天的安排，30 秒内记下一条待办，手机和电脑都能访问，下次进入数据仍在。

**使用者**：仅本人。无注册登录、无第二角色、无协作。

**典型场景**

| 场景 | 设备 | 要完成的事 |
|---|---|---|
| 出门前扫一眼今天 | 手机 | 看今日课程、下一节倒计时、待办 |
| 课间想起一件事 | 手机 | 快速新建一条日程 |
| 晚上回宿舍安排明天 | 电脑 | 编辑日程、查看整周课表 |
| 想知道某节课在哪上 | 两者 | 查课表周视图 |

**数据边界（已与用户确认接受）**：第一版不用数据库。日程存浏览器 localStorage——同一设备同一浏览器内持久；换浏览器/设备/清缓存后日程不保留；手机与电脑各自的日程互不可见。课表是内置静态数据，因此所有设备看到的课表一致。

## 2. 整体页面框架和导航结构

- **形态**：托管网页（沿用现有站点），手机与电脑浏览器访问，无后端接口。
- **页面**：3 个——首页 `#/`、课表 `#/timetable`、日程 `#/tasks`。Hash 路由。
- **导航**：断点仅 640px 一个。
  - 手机（<640px）：底部 3 Tab（首页/课表/日程），顶栏一行显示日期+周次。
  - 桌面（≥640px）：顶部水平 Tab，内容区最大宽 960px 居中。
- **视觉**：「深空驾驶舱」体系——底 `#070B14`、面板 `#0E1524`、描边 `#1E2B45`；青 `#3DD6F5`=当前/进行中，橙 `#FF8A3D`=临期/逾期，绿 `#4ADE80`=已完成；时间数字等宽。
- **全局交互约定**：
  1. 新建/编辑一律浮层：桌面居中 Modal（480px），手机底部抽屉（下拉关闭）；`Esc`/点遮罩关闭。
  2. 删除用 Toast「已删除 · 撤销」5 秒，不弹确认框。
  3. 每页具备空态（图标+引导语+主按钮）、骨架屏、错误态重试。
  4. 表单失焦即校验，非法时提交按钮禁用。

## 3. 首页的内容结构

自上而下 5 个区块：

1. **顶栏**：`2026-09-20 周日 · 第2周`；日期可点开只读迷你月历（标记有课/有日程的日期）。
2. **下一节卡（Hero）**：课程名+地点+「还有 HH:MM:SS」秒级倒计时；今日无课→「今日无课」；已全部上完→「今日课程已结束」。
3. **今日课程**：纵向时间轴；已过节次置灰，当前节青色左边框+「进行中」徽标；每行显示起止时间、课程名、地点。
4. **今日日程**：未完成置顶，点击圆圈直接勾选完成（划线置灰沉底）；条目显示时间段（若有）。
5. **临近日程**：明天起 3 天内，按日期分组，橙色日期标签。

**首页概念图（手机）**

```
┌──────────────────────────┐
│ 9月20日 周日 · 第2周   📅 │
├──────────────────────────┤
│  下一节 14:00            │
│   数学基础 · 科研一号楼1001│
│   还有 02:31:45           │
├──────────────────────────┤
│ 今日课程                  │
│  08:00 ─ 法国歌剧史…  ✓置灰│
│  10:00 ─ 综合法语实训  ✓置灰│
│  14:00 ┃ 数学基础   [进行中]│
│  15:50 ─ 体育(1)          │
├──────────────────────────┤
│ 今日日程 (2)         ＋   │
│  ○ 交法语作业   19:50-21:30│
│  ● 取快递       （已完成） │
├──────────────────────────┤
│ 临近日程                  │
│  明天 9/21 ▸ 组会 14:00   │
├──────────────────────────┤
│  [首页]   [课表]   [日程]  │
└──────────────────────────┘
```

## 4. 每个模块的用途

| 模块 | 用途 | 读写性质 |
|---|---|---|
| 首页 | 今日总览：下一节、今日课程、今日/临近日程一屏看全 | 课程只读；日程可读+可勾选完成 |
| 课表 | 每周 12 条课程的固定展示，回答"今天/某天有什么课、在哪上" | 完全只读 |
| 日程 | 个人待办的增删改查与完成管理，按逾期/今天/临近分组 | 读写（localStorage） |

## 5. 每个模块第一版最必要的数据和操作

### 5.1 校历参数（内置常量）
- 第 1 周周一 = **2026-09-07**；教学周 **1–14 周**；无单双周。
- 作息时间表（节次网格以此为准）：
  1 08:00-08:45 / 2 08:50-09:35 / 3 09:50-10:35 / 4 10:40-11:25 / 5 11:30-12:15 / 6 14:00-14:45 / 7 14:50-15:35 / 8 15:50-16:35 / 9 16:40-17:25 / 10 17:30-18:15 / 11 19:00-19:45 / 12 19:50-20:35 / 13 20:40-21:25 / 14 21:30-22:15。

### 5.2 课表数据（内置常量数组，12 条）

| 星期 | 节次 | 课程 | 地点 |
|---|---|---|---|
| 一 | 1-2 | 法国歌剧史与作品赏析 | 教学一号楼3004 |
| 一 | 3-4 | 综合法语实训(1) | 教学二号楼4003 |
| 一 | 6-7 | 综合法语(1) | 教学一号楼5005 |
| 一 | 8-9 | 体育(1) | （无地点） |
| 二 | 1-2 | 基础英语(1) | 教学一号楼4003 |
| 二 | 3-5 | 大学计算机基础 | 计算机房（R1-4090） |
| 二 | 6-7 | 综合法语(1) | 教学一号楼5005 |
| 三 | 3-4 | 综合法语(1) | 教学一号楼5005 |
| 四 | 3-4 | 航空航天概论A | 教学一号楼2003 |
| 四 | 6-7 | 数学基础 | 科研一号楼1001 |
| 四 | 11-12 | 综合法语(1) | 教学一号楼5005 |
| 五 | 6-7 | 综合法语实训(1) | （无地点） |

记录结构：`{ day(1-7), startSection, endSection, name, room, color }`。
操作：仅展示与点看详情，无增删改。

### 5.3 日程数据（localStorage）
- 键：`schedule.tasks.v1`；结构：`{ id, title, date, startTime?, endTime?, location?, note?, done, createdAt }`。
- 字段规则：标题必填；日期必填；起止时间选填且成对校验（有始必有终）；地点、备注选填。
- 操作：新建、编辑、删除（可撤销）、勾选完成/取消完成、按 全部/未完成/已完成 筛选。

### 5.4 课表页面布局
- 桌面：7 列 × 14 节网格，课程色块跨连堂行，红色 now 线。
- 手机：单日纵向时间轴 + 顶部星期胶囊横滑（默认今天），左右滑动切日。
- 概念图见 `PRD_第一阶段.md` 7.2 节。

### 5.5 日程页面布局
- 段控件（全部/未完成/已完成）+ 分组列表：**逾期 → 今天 → 明天 → 本周内 → 未来**；逾期橙条、今日/临近青条；右下悬浮「＋」。
- 概念图见 `PRD_第一阶段.md` 7.3 节。

## 6. 首页、今日计划和各模块之间的关系

- **课表 → 首页**：首页「今日课程/下一节」是对内置课表按「当前周次(≤14) + 今天星期几 + 当前时刻」过滤排序的结果，不另存副本。周次 >14 或假期时首页显示"今日无课"。
- **日程 → 首页**：首页「今日日程」= `date == 今天` 的分组视图（未完成置顶）；「临近日程」= `明天 ≤ date ≤ 今天+3` 且未完成的只读摘要；逾期项不进首页，只在日程页橙条呈现。
- **首页 → 日程**：点首页日程条目跳转日程页并定位高亮该条；首页「＋」与日程页「＋」打开同一个新建浮层组件。
- **写入唯一入口在日程页/浮层**；首页只做勾选完成这一个写操作；课表页零写入。三页读取同一份 localStorage，多标签页打开时经 `storage` 事件同步。

## 7. 第一版必须完成的功能

1. 三页 Hash 路由 + 手机底部 Tab / 桌面顶部 Tab 导航。
2. 周次自动推算并全局展示；跨零点自动刷新日期分组。
3. 首页五区块（顶栏、下一节倒计时、今日课程时间轴、今日日程、临近日程）及置灰/进行中/高亮状态。
4. 课表：桌面周网格 + 手机单日轴双布局；12 条课程录入；now 线；点块看只读详情。
5. 日程：新建/编辑（浮层表单+校验）、删除（Toast 撤销）、勾选完成、三态筛选、五段分组列表。
6. localStorage 持久化 + 多标签页同步；断网可完整使用。
7. 空态、骨架屏、错误态重试。
8. 手机 360px～桌面 1440px 无横向滚动、无布局破版。

## 8. 第一版暂时不做的功能

- 注册登录、任何账号体系。
- 社交/共享/协作。
- 云数据库、后端接口、跨设备日程同步。
- 消息推送/提醒通知（仅视觉高亮）。
- 课程增删改、课表导入导出、日程导出备份。
- 日程重复规则（每周重复等）。
- 课程/日程冲突检测与提示。
- 科研、健身、设置模块（见总纲存档，日后另议）。

## 9. 验收标准（可实际检查）

**数据与持久**
1. 新建 3 条日程后关闭标签页再打开，3 条仍在且状态不变。
2. 在 Chrome 添加的日程，Edge/手机上看不到，且无报错——符合第 1 节声明的边界。
3. 查看源码或内存：课表常量恰为 5.2 表 12 条，字段值与表一致。

**周次与今日**
4. 2026-09-20（第 2 周周日）打开首页：周次显示"第2周"；无课则 Hero 显示"今日无课"。
5. 把系统时间改到 2026-12-28（>14 周）：首页"今日无课"，课表顶部显示"本周无教学安排（第 X 周）"。
6. 工作日上午 9:00 打开首页：第二节之前的课全部置灰，08:00-08:45 若已过则不出现"进行中"，下一节卡指向 09:50 的课且倒计时每秒跳动。

**课表**
7. 桌面 1280px：周一列 1-2 节出现「法国歌剧史与作品赏析」，色块跨两行；周四 11-12 节出现「综合法语(1)」。
8. 手机 375px：默认停在"今天"胶囊；横滑到周二可见 3-5 节「大学计算机基础」跨三行。
9. 点任意课程块弹出详情含星期、节次、起止时间、地点；浮层内无任何编辑按钮。

**日程**
10. 标题留空提交：提交按钮禁用且标题字段出现失焦报错。
11. 只填开始时间不填结束时间：提交被拦截并提示成对填写。
12. 昨天日期的日程出现在「逾期」组且带橙色竖条；今天的带青色竖条。
13. 删除一条日程，5 秒内点 Toast「撤销」可恢复；5 秒后条目彻底消失且 localStorage 中无残留。
14. 勾选完成后条目划线置灰并沉到所在组底部；切「未完成」筛选不再显示它。
15. 列表为空时显示空态插画与引导按钮，点按钮直接打开新建浮层。

**一致性与健壮**
16. 同一浏览器开两个标签页，A 页新建日程，B 页不刷新即可见（`storage` 事件）。
17. 手工把 `schedule.tasks.v1` 写成非法 JSON：页面显示错误态并可一键清空重置，不白屏。
18. 三页在 360 / 640 / 1024 / 1440px 四档宽度截图检查：无横向滚动条、Tab 不重叠、Modal 不超屏。
