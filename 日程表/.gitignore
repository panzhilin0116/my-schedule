# 个人图片资料：只留本地，不入库
校历.jpg
微信图片_*.jpg

# 系统与编辑器临时文件
.DS_Store
Thumbs.db
desktop.ini
*.tmp
*.log

# 无头浏览器测量脚本的临时 profile 与产物（含浏览器 cookie，绝不入库）
.measure/

# 发布产物：由 dev/build-v2.mjs 生成，只用于打包上传
v2/dist/

# Qoder Sites 描述符：每次 prepare/publish 都会变，内含云端资源 ID 与首页快照，只留本地
*.qoder.site

# OCR 冒烟测试的临时图与产物：只留本地，不入库
.smoke/
dev/smoke/
