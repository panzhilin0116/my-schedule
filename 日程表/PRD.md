# 个人日程管理系统 · PRD（第一版）

版本：v2.2
日期：2026-09-21
状态：定稿。**本文档是后续开发的唯一依据。** 旧六模块总纲已存档为 `PRD_总纲_v1.0存档.md`，其中科研/健身/设置与云数据库方案不在第一版范围，留待日后另立版本讨论；需求与交互设计过程稿见 `PRD_第一阶段.md`。

> **v2.1 变更记录（2026-09-21，两项均已实现并上线）**
> 1. **多空间隔离**：站点公开可访问，任何访客进入即自动获得独立空间，日程按空间隔离（原 v2.0「仅本人使用」口径作废，见 §1、§5.6）。
> 2. **课表去内置化**：v2.0 §5.2 的 12 条内置只读课程数据整体下线；课表改为每个空间空白起步、自行增删改（见 §5.2）。节次模板与校历仍全站统一。
>
> **v2.2 变更记录（2026-09-21，方案定稿、代码未动）**
> 1. 新增 §5.7「一键导入课表（粘贴文字）」：用户复制课表文本粘贴 → 本地解析 → 可编辑预览 → 确认后批量填充本空间课表。原「截图识别」路线作废（识别照片需云端或低质量本地 OCR，粘贴文字纯本地即可高质量完成）。
> 2. 曾定稿又撤下的「大一新生按班级导入问卷」（旧 §5.7）**不在本版范围**，底稿存 git `2cc2b5f`，是否重启由用户另行决定。

---

## 1. 产品目标和使用场景

**目标**：一名大学生管理自己的课程与日程：打开网页即可看今天的安排，30 秒内记下一条待办，手机和电脑都能访问，下次进入数据仍在。

**使用者**：站点公开，任何人打开链接即为一名使用者。**无注册、无登录、无第二角色、无协作**；每位访客进入时浏览器自动生成一个私有「空间」，不同空间的数据互不可见（见 §5.6）。产品仍按"单人管理自己"设计，不做共享。

**典型场景**

| 场景 | 设备 | 要完成的事 |
|---|---|---|
| 出门前扫一眼今天 | 手机 | 看今日课程、下一节倒计时、待办 |
| 课间想起一件事 | 手机 | 快速新建一条日程 |
| 晚上回宿舍安排明天 | 电脑 | 编辑日程、查看整周课表 |
| 想知道某节课在哪上 | 两者 | 查课表周视图 |
| 第一次进来还没有课 | 两者 | 在课表页把本学期课程录进自己的空间 |
| 教务系统里已有整张课表 | 两者 | 复制课表文本 →「一键导入」粘贴解析 → 确认预览 → 填进课表（§5.7，待实现） |

**数据边界（已与用户确认接受）**：第一版不用数据库。日程与课程都存浏览器 localStorage，且**按空间隔离**——同一设备同一浏览器内持久；换浏览器/设备/清缓存后数据不保留；手机与电脑各自的空间互不可见。所有访客进入时看到的都是**空白课表**，需自行录入，任何人看不到别人的数据。

## 2. 整体页面框架和导航结构

- **形态**：托管静态网页（沿用现有站点），手机与电脑浏览器访问，无后端接口、无账号体系。空间隔离与数据持久化全部在浏览器 localStorage 完成。
- **页面**：3 个——首页 `#/`、课表 `#/timetable`、日程 `#/tasks`。Hash 路由。
- **导航**：断点仅 640px 一个。
  - 手机（<640px）：底部 3 Tab（首页/课表/日程），顶栏一行显示日期+周次。
  - 桌面（≥640px）：208px 左侧导航栏 + 内容区最大宽 960px 居中。
- **视觉**：深色液态玻璃（Liquid Glass）设计体系——具体色值与材质规范以 `v2/styles.css` 顶部 CSS token（`--base`、`--accent`、`--warn`、`--ok`、`--danger` 等）为准，由 `v2/tests/design.test.mjs` 契约测试把关。强调色低饱和，不抢内容；时间数字等宽。
- **全局交互约定**：
  1. 新建/编辑一律浮层：桌面居中 Modal（480px），手机底部抽屉（下拉关闭）；`Esc`/点遮罩关闭。
  2. 删除用 Toast「已删除 · 撤销」5 秒，不弹确认框。
  3. 每页具备空态（图标+引导语+主按钮）、骨架屏、错误态重试。
  4. 表单失焦即校验，非法时提交按钮禁用。

## 3. 首页的内容结构

自上而下 5 个区块：

1. **顶栏**：`2026-09-20 周日 · 第2周`；日期可点开只读迷你月历（标记有课/有日程的日期）。右侧有空间指示器：`空间` 标签 + 当前空间短 ID（末 4 位）+「新建空间」按钮（见 §5.6）。
2. **下一节卡（Hero）**：课程名+地点+「还有 HH:MM:SS」秒级倒计时；今日无课→「今日无课」；已全部上完→「今日课程已结束」。
3. **今日课程**：纵向时间轴；已过节次置灰，当前节青色左边框+「进行中」徽标；每行显示起止时间、课程名、地点。
4. **今日日程**：未完成置顶，点击圆圈直接勾选完成（划线置灰沉底）；条目显示时间段（若有）。
5. **临近日程**：明天起 3 天内，按日期分组，橙色日期标签。

**首页概念图（手机，课程名仅为排版示例）**

```
┌──────────────────────────┐
│ 9月20日 周日 · 第2周   📅 │
├──────────────────────────┤
│  下一节 14:00            │
│   数学基础 · 科研一号楼1001│
│   还有 02:31:45           │
├──────────────────────────┤
│ 今日课程                  │
│  08:00 ─ 法国歌剧史…  ✓置灰│
│  10:00 ─ 综合法语实训  ✓置灰│
│  14:00 ┃ 数学基础   [进行中]│
│  15:50 ─ 体育(1)          │
├──────────────────────────┤
│ 今日日程 (2)         ＋   │
│  ○ 交法语作业   19:50-21:30│
│  ● 取快递       （已完成） │
├──────────────────────────┤
│ 临近日程                  │
│  明天 9/21 ▸ 组会 14:00   │
├──────────────────────────┤
│  [首页]   [课表]   [日程]  │
└──────────────────────────┘
```

## 4. 每个模块的用途

| 模块 | 用途 | 读写性质 |
|---|---|---|
| 首页 | 今日总览：下一节、今日课程、今日/临近日程一屏看全 | 课程只读；日程可读+可勾选完成 |
| 课表 | 展示并管理**本空间**的课程，回答"今天/某天有什么课、在哪上" | 读写（localStorage）：新增、编辑、删除课程；块详情可编辑 |
| 日程 | 个人待办的增删改查与完成管理，按逾期/今天/临近分组 | 读写（localStorage） |

## 5. 每个模块第一版最必要的数据和操作

### 5.1 校历参数（内置常量）
- 第 1 周周一 = **2026-09-07**；教学周 **1–14 周**；无单双周。
- 作息时间表（节次网格以此为准）：
  1 08:00-08:45 / 2 08:50-09:35 / 3 09:50-10:35 / 4 10:40-11:25 / 5 11:30-12:15 / 6 14:00-14:45 / 7 14:50-15:35 / 8 15:50-16:35 / 9 16:40-17:25 / 10 17:30-18:15 / 11 19:00-19:45 / 12 19:50-20:35 / 13 20:40-21:25 / 14 21:30-22:15。

### 5.2 课表数据（localStorage，按空间隔离）

> v2.0 的"内置常量数组 12 条"已于 2026-09-21 整体下线（用户拍板：所有空间空白起步，本人的课也由本人重新录入）。

- 键：`schedule.courses.v1.<空间 ID>`（见 §5.6）；每门课结构：`{ id('c_' 前缀), name, day(1-7), startSection, endSection, room?, color, createdAt }`。
- v2.2 §5.7 生效后再扩展两个可选字段：`weeks?`、`importedFrom?`；缺省值与旧数据兼容规则见 §5.7。
- 字段规则：名称必填；星期必选（周一~周日）；节次取 5.1 作息模板 1–14，结束节次不得早于开始节次；地点选填；颜色从固定 6 色调色板（蓝/紫/绿/橙/青/粉）中选，默认轮询分配。
- 冲突检测：同星期节次区间与**本空间其他课程**重叠时，浮层内报错「与「XX」时间冲突」并禁用提交（编辑自身不算冲突）。
- 操作：课表页头部「＋ 添加课程」新建；点课程块弹详情，详情含节次起止、地点、覆盖周次说明与「编辑 / 删除」按钮；删除走 Toast「已删除 · 撤销」5 秒（含在编辑浮层内点删除的路径，撤销后即时回填网格）。
- 空态：新空间课表为空白，显示引导空态「这个空间的课表还是空的」+「添加第一节课」按钮。
- 节次模板与校历为全站统一常量，本期不做按空间自定义（已确认）。

### 5.3 日程数据（localStorage，按空间隔离）
- 键：`schedule.tasks.v1.<空间 ID>`；结构：`{ id, title, date, startTime?, endTime?, location?, note?, done, createdAt }`。
- 字段规则：标题必填；日期必填；起止时间选填且成对校验（有始必有终）；地点、备注选填。
- 操作：新建、编辑、删除（可撤销）、勾选完成/取消完成、按 全部/未完成/已完成 筛选；筛选态切页后保持。

### 5.4 课表页面布局
- 桌面：7 列 × 14 节网格，课程色块跨连堂行，红色 now 线。
- 手机：单日纵向时间轴 + 顶部星期胶囊横滑（默认今天），左右滑动切日。
- 概念图见 `PRD_第一阶段.md` 7.2 节。

### 5.5 日程页面布局
- 段控件（全部/未完成/已完成）+ 分组列表：**逾期 → 今天 → 明天 → 本周内 → 未来**；逾期橙条、今日/临近青条；右下悬浮「＋」。
- 概念图见 `PRD_第一阶段.md` 7.3 节。

### 5.6 空间机制（纯前端，无注册）
- 首次打开站点：自动生成空间 ID（格式 `sp_<时间戳36进制>_<随机6位>`，存 localStorage 键 `schedule.spaceId`），并落到当前 URL 会话；全程无需注册/登录。
- 所有个人数据键以空间 ID 作命名空间：`schedule.tasks.v1.<空间 ID>`、`schedule.courses.v1.<空间 ID>`；不同空间互不可见。
- 顶栏空间指示器：「空间」标签 + 短 ID（末 4 位大写，悬停显示完整 ID）+「新建空间」按钮。
- **新建空间**：二次确认后**清空本浏览器内当前空间的全部数据**并切换到全新空间（旧空间 ID 不保留、无法切回）。
- 坏数据保护：某空间数据被写成非法 JSON 时，页面显示错误态并可一键清空重置（沿用 §7 原有约定）。

### 5.7 一键导入课表 · 粘贴文字（v2.2 新增，**待实现**）

**定位**：把"一节一节手输"变成"整张课表复制粘贴进来"。**只做文字粘贴，不做图片/截图识别**（截图路线作废，见 §8）。解析 100% 在本机浏览器完成，粘贴内容不发任何网络请求，与 §5.6 空间隔离一致：导入结果只落当前空间。

**入口**
- 课表页头部「＋ 添加课程」旁增设次级按钮**「⇋ 一键导入」**（常驻，与课表空/非空无关）。
- 课表空态在现有引导按钮下追加次级链接「粘贴文字导入课表」，打开同一浮层。

**浮层流程（三步，同一浮层内）**
1. **粘贴**：大号多行输入框，顶部提示文案固定为**「请复制您的课表并粘贴」**，下方灰色小字补充：`支持从教务系统/课程表 App/Excel 复制后直接粘贴，每行一条安排；内容仅在本机解析，不会上传`。底部主按钮〔解析〕（内容为空时禁用）。
2. **预览确认**：解析结果以**可编辑清单**呈现，每行 = 一条识别出的课程（字段同 §5.2：名称/星期/节次起止/地点/周次/颜色），行内可直接改正；三种状态徽标——〔已识别〕〔需修正〕（星期或节次缺失/非法，红色标出缺哪项，不修正该行无法勾选）〔冲突〕（与本空间已有课或清单内其他行按 §5.7"冲突规则"判定重叠，默认不勾选）。清单下方列出**「未能识别的内容」**折叠区，逐行回显原文，**永不静默丢弃**；一行含多个上课日（如教务"班课表"整行）拆为多条预览行。主按钮〔确认导入 N 门课〕，N = 勾选且状态合法的行数；另提供〔取消〕返回粘贴步骤（可改原文重解析）。
3. **写库**：勾选项逐条走 §5.2 校验 + `upsertCourse` 写入本空间，每条打 `importedFrom: 'import'` 标记；完成后 Toast「已导入 N 门课 · 可随时编辑」并落到课表页。存储写满报 StoreError Toast（沿用现有）。

**解析规则（按行启发式，纯本地）**
- 星期：`周一/星期一/礼拜一/周1`（周一=1…周日=7）；无法判定星期的行 →〔需修正〕。
- 节次：`第3-4节/3-4节/030405节` 类节次号；或**时间段** `08:00-09:35/14：00~15：35`，按 §5.1 模板起始时刻换算为节次号（就近匹配开始节，换算不出 →〔需修正〕）。
- 周次：`第(1)-(16)周/1-16周/单周/双周` → 可选字段 `weeks {from, to, parity}`；缺省视为全教学周。
- 地点：匹配含 `楼/室/房/馆/区/中心` 或 `教学楼/实验/机房/体育` 等 token 的片段；剩下的第一段为课程名；两者难分时标〔需修正〕交给人。
- 表头噪声（`星期/时间/上午/下午/晚上` 及纯分隔线）、空行：忽略，不进未识别区。
- **来源探测（样例 1 教训，见下）**：解析先判定文本形态——若全文**没有任何星期/节次/时间 token**、只有"课名+地点"对（网格复制丢位形态），不进入逐行预览，而是给出明确提示「这种复制方式丢失了上课时间信息」，并引导两条出路：① 换保留网格结构的来源重新复制（推荐：电脑浏览器打开教务/用课表 App 的"导出/分享课表"功能）；②〔半自动模式〕：程序已认出的"课名+地点"逐条预填进添加表单，用户只需为每条选星期和节次（比从零手输快）。半自动模式是否纳入首期**待用户裁定**。
- 解析纯规则实现、零依赖；以用户提供的**真实粘贴文本**做测试夹具，样例逐份归档如下。

**真实样例档案**
- **样例 1（2026-09-22，手机端教务课表页长按复制）**：结构 = 表头（`星期节次` + `一 9.21 … 日 9.27`）→ 节次号 `1…14` 竖排 → 之后为按"周列从上到下、列与列之间按周一→周日"平铺的 `课名↵地点` 对（本例 14 对）。**实测结论：课程色块在页面上是绝对定位绘制，纯文本复制后星期/节次信息完全丢失，逐行规则无法把课放回网格**；顺序虽呈列主序，但无每日边界锚点，不可靠。该样例作为"来源探测"的负例夹具保留；同时它证明了"课名+地点"两行一对的识别本身可靠，是半自动模式的可用输入。
- 样例 2、3（**待提供**）：需来自**保留结构**的复制源（电脑端教务网页 / 课表 App 导出文本 / Excel 复制），到位一份细化一份解析夹具。

**冲突规则（随本期升级 §5.2）**
- 仅当两门课**星期相同、节次区间重叠、且周次范围有交集**（`all`/同 parity 相交、`odd` vs `even` 不相交）才算冲突。
- 预览清单内部互冲、与已有课冲，均标〔冲突〕默认不勾；用户删改其一后可重新判定。

**导入后可管理性**
- 导入课与手加课完全同权（编辑/删除/撤销照旧），详情浮层对带标记课显示「导入的课程」徽标。
- 「一键导入」浮层检测到空间已有导入标记课时，粘贴步骤上方提供〔清除全部导入课〕（二次确认，只删带标记的课，手加课不动，无撤销、重导即恢复）。

**明确不做**
- 图片/截图/PDF/.xlsx 文件导入；剪贴板自动读取（一律用户手动粘贴）；云端解析兜底。
- 跨空间共享导入结果；导入模板的"官方课表"分发（属已撤下的大一方案）。

## 6. 首页、今日计划和各模块之间的关系

- **课表 → 首页**：首页「今日课程/下一节」是对**本空间课表**（§5.2）按「当前周次(≤14) + 今天星期几 + 当前时刻」过滤排序的结果，不另存副本。周次 >14 或假期、或空间尚无课程时，首页显示"今日无课"。
- **日程 → 首页**：首页「今日日程」= `date == 今天` 的分组视图（未完成置顶）；「临近日程」= `明天 ≤ date ≤ 今天+3` 且未完成的只读摘要；逾期项不进首页，只在日程页橙条呈现。
- **首页 → 日程**：点首页日程条目跳转日程页并定位高亮该条；首页「＋」与日程页「＋」打开同一个新建浮层组件。
- **写入入口**：日程页/浮层可增删改日程；课表页/浮层可新增、编辑、删除课程（同空间）。首页只做勾选完成这一个写操作，不写课程/日程本体。三页读取当前空间的同一份 localStorage，多标签页打开时经 `storage` 事件同步。

## 7. 第一版必须完成的功能

1. 三页 Hash 路由 + 手机底部 Tab / 桌面左侧栏导航。
2. 周次自动推算并全局展示；跨零点自动刷新日期分组。
3. 首页五区块（顶栏、下一节倒计时、今日课程时间轴、今日日程、临近日程）及置灰/进行中/高亮状态。
4. 课表：桌面周网格 + 手机单日轴双布局；本空间课程的新增/编辑/删除（删除可撤销）+ 时间冲突检测；now 线；点块看详情并可编辑。空空间显示引导空态。
5. 日程：新建/编辑（浮层表单+校验）、删除（Toast 撤销）、勾选完成、三态筛选、五段分组列表。
6. localStorage 持久化 + 多标签页同步 + 空间隔离；断网可完整使用。
7. 空态、骨架屏、错误态重试。
8. 手机 360px～桌面 1440px 无横向滚动、无布局破版。
9. 多空间：首次进入自动生成私有空间；日程/课程按空间命名空间存储、互不可见；顶栏空间指示器 + 新建（重置）空间。
10. （**待实现**，v2.2）一键导入课表（粘贴文字）：方案见 §5.7；开工前置条件 = 用户提供真实粘贴文本样例并明确下令。

## 8. 第一版暂时不做的功能

- 注册登录、任何账号体系。
- 社交/共享/协作（空间之间永久互不可见）。
- 云数据库、后端接口、跨设备日程同步。
- 消息推送/提醒通知（仅视觉高亮）。
- 课表**导出**、日程导出备份（课表**导入**已由 §5.7 于 v2.2 引入，仅支持粘贴文字）。
- **课表截图/图片识别添加**（2026-09-21 提出后暂缓，同日由用户改定为 §5.7「粘贴文字导入」路线，图片路线**作废**：云端识别涉后端与隐私边界、本地 OCR 识别率不达标）。
- 图片/PDF/.xlsx 文件导入与剪贴板自动读取（§5.7 明确不做项）。
- 节次模板、校历按空间自定义（当前全站统一 5.1 模板，已确认保持统一）。
- 日程重复规则（每周重复等）。
- 日程与课程之间的冲突检测（课程之间的冲突检测已实现，见 §5.2）。
- 科研、健身、设置模块（见总纲存档，日后另议）。

## 9. 验收标准（可实际检查）

**数据与持久**
1. 新建 3 条日程后关闭标签页再打开，3 条仍在且状态不变。
2. 在 Chrome 添加的日程/课程，Edge/手机上看不到，且无报错——符合第 1 节声明的边界。
3. 点顶栏「新建空间」并二次确认后：课表与日程均回到空白、显示引导空态；旧空间数据已从本浏览器清除，无法读回。
4. 空间 A 录入课程与日程后，切到新建空间 B：B 完全看不到 A 的任何数据；两空间数据互不覆盖。

**周次与今日**
5. 2026-09-20（第 2 周周日）打开首页：周次显示"第2周"；无课则 Hero 显示"今日无课"。
6. 把系统时间改到 2026-12-28（>14 周）：首页"今日无课"，课表顶部显示"本周无教学安排（第 X 周）"。
7. 工作日上午 09:50 打开首页：第 1-2 节连堂（08:00-09:35）已置灰，下一节卡指向 09:50 的课且倒计时每秒跳动。

**课表（先在本空间录入若干课程再验证）**
8. 桌面 1280px：录入一门周一 1-2 节的课后，其色块出现在周一列且跨两行；再录一门周四 11-12 节的课，出现在对应位置。
9. 手机 375px：默认停在"今天"胶囊；横滑到录了课的那天，连堂课程块按节次行数正确跨行。
10. 点任意课程块弹详情，含星期、节次、起止时间、地点，并有「编辑」「删除」按钮。
11. 时间冲突检测：新建一门与已有课程同星期、节次重叠的课，浮层内提示「与「XX」时间冲突」且提交按钮禁用；改成不重叠的节次后即可提交。
12. 删除（含在编辑浮层内点删除）：课程即时从网格消失、顶部出现「已删除 · 撤销」Toast；5 秒内点撤销，课程即时回填网格与 localStorage。

**日程**
13. 标题留空提交：提交按钮禁用且标题字段出现失焦报错。
14. 只填开始时间不填结束时间：提交被拦截并提示成对填写。
15. 昨天日期的日程出现在「逾期」组且带橙色竖条；今天的带青色竖条。
16. 删除一条日程，5 秒内点 Toast「撤销」可恢复；5 秒后条目彻底消失且 localStorage 中无残留。
17. 勾选完成后条目划线置灰并沉到所在组底部；切「未完成」筛选不再显示它。
18. 列表为空时显示空态插画与引导按钮，点按钮直接打开新建浮层。

**一致性与健壮**
19. 同一浏览器开两个标签页，A 页新建日程，B 页不刷新即可见（`storage` 事件）。
20. 手工把当前空间的 `schedule.tasks.v1.<空间 ID>` 写成非法 JSON：页面显示错误态并可一键清空重置，不白屏。
21. 三页在 360 / 640 / 1024 / 1440px 四档宽度截图检查：无横向滚动条、Tab 不重叠、Modal 不超屏。

**一键导入课表（§5.7，v2.2 待实现）**
22. 课表页头部有「⇋ 一键导入」按钮；点开后顶部提示文案为**「请复制您的课表并粘贴」**，输入框为空时〔解析〕禁用。
23. 粘贴含多行课程文本（覆盖 §5.7 解析规则各形态：星期/节次区间/时间/地点/周次）→〔解析〕→ 预览清单逐行回显识别结果，字段与原文对应正确（真实样例夹具到位后补对应断言）。
24. 预览某行标〔需修正〕（缺星期或节次）时该行无法勾选、〔确认导入〕的 N 不计它；在预览内改对后转为〔已识别〕可勾选。
25. 含无法解析的原文行进入「未能识别的内容」折叠区，逐字回显且不静默丢弃；纯表头/空行不进该区域。
26. 勾选合法行〔确认导入 N 门课〕：课程即时出现在课表网格，数量与 N 一致；带 `importedFrom:'import'` 且详情浮层显「导入的课程」徽标。
27. 导入课与已有课按 §5.7 冲突规则判定：星期相同、节次区间重叠、周次有交集 → 预览标〔冲突〕默认不勾；`odd` 对 `even`（或范围不相交）不算冲突、可并存。
28. 导入后在课表页对某门导入课点删除 → Toast 撤销可恢复；〔清除全部导入课〕二次确认后，只删带 `importedFrom` 的课，手加课全部保留，无撤销。
29. 整个导入流程无任何网络请求（DevTools Network 确认）；解析与预览均在本机完成，粘贴内容不落 localStorage 原文（只落解析后的课程）。
30. 负例夹具（样例 1 文本）：粘贴"丢位网格"文本 → 不进入逐行预览，显示「这种复制方式丢失了上课时间信息」提示与换来源指引（若半自动模式获批，则列出 14 条"课名+地点"预填表单待选星期/节次）。
